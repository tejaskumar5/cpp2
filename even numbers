#include<iostream>
using namespace std;

int main()
{
	int n;
	
	cout << "\nEnter a Value =  ";
	cin >> n;
	
	cout << "\nEven Numbers from 0 to " << n << " are\n"; 
	for(int i = 0; i <= n; i++)
  	{
  		if ( i % 2 == 0 )
  		{
  			cout << i <<" ";
		}	
  	}
		
 	return 0;
}
